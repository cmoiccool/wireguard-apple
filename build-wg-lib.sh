#!/bin/sh

# This script builds the WireGuard library for iOS and macOS using the
# iOS and macOS SDKs. It is intended to be run from the root of the
# WireGuard source tree.

set -e

echo "Building WireGuard library for iOS and macOS"
